{
  "name": "isorpg",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "2.5D isometric hybrid RPG & settlement builder — mobile-first, zero-asset procedural Three.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "rm -rf .qc-emit && tsc -p tests/tsconfig.json && echo '{\"type\":\"commonjs\"}' > .qc-emit/package.json && node scripts/audit-ui.cjs && node scripts/verify-rig.cjs && node .qc-emit/tests/qc.test.js && node scripts/gen-wiki.cjs",
    "preview": "vite preview --port 4173",
    "test:smoke": "npm run build && node scripts/smoke.cjs && node scripts/visual-regress.cjs",
    "models:manifest": "node scripts/gen-model-manifest.cjs",
    "prebuild": "node scripts/gen-model-manifest.cjs",
    "predev": "node scripts/gen-model-manifest.cjs",
    "wiki": "node scripts/gen-wiki.cjs",
    "visual": "node scripts/visual-regress.cjs",
    "visual:update": "node scripts/visual-regress.cjs --update",
    "audit": "node scripts/audit.cjs",
    "audit:quick": "node scripts/audit.cjs --quick",
    "verify:pwa": "node scripts/verify-pwa-template.cjs",
    "icons:app": "node scripts/gen-app-icons.cjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "playwright-core": "^1.62.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
